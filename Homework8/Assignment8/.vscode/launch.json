{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "调试 RopeSim (CodeLLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": ["-s", "64"],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false,
            "terminal": "integrated"
        }
    ]
}